import { Download,RotateCcw,Search } from 'lucide-react';
type Option={value:string;label:string};
const defaults:Option[]=[{value:'revenue',label:'Facturación'},{value:'name',label:'Nombre'},{value:'orders',label:'Cantidad'}];
export function AnalyticsToolbar({search,setSearch,order,setOrder,filter,setFilter,filterOptions=[],resultCount,onExport,onReset,placeholder='Buscar…',orderOptions=defaults,direction='desc',setDirection}:{search:string;setSearch:(v:string)=>void;order:string;setOrder:(v:string)=>void;filter:string;setFilter:(v:string)=>void;filterOptions?:string[];resultCount:number;onExport:()=>void;onReset:()=>void;placeholder?:string;orderOptions?:Option[];direction?:'asc'|'desc';setDirection?:(v:'asc'|'desc')=>void}){return <div className="analytics-toolbar-wrap"><div className="analytics-toolbar"><label><Search/><input value={search} onChange={e=>setSearch(e.target.value)} placeholder={placeholder} aria-label="Buscar"/></label><select value={order} onChange={e=>setOrder(e.target.value)} aria-label="Criterio de orden">{orderOptions.map(x=><option key={x.value} value={x.value}>Ordenar por: {x.label}</option>)}</select>{setDirection&&<select value={direction} onChange={e=>setDirection(e.target.value as 'asc'|'desc')} aria-label="Dirección del orden"><option value="desc">Mayor a menor</option><option value="asc">Menor a mayor</option></select>}<select value={filter} onChange={e=>setFilter(e.target.value)} aria-label="Filtrar"><option value="">Filtrar: todos</option>{filterOptions.map(x=><option key={x} value={x}>{x==='positive'?'Crecimiento':x==='negative'?'Caída':x}</option>)}</select></div><div className="analytics-toolbar-actions"><strong>{resultCount.toLocaleString('es-AR')} resultados</strong><button onClick={onReset}><RotateCcw/>Limpiar</button><button onClick={onExport}><Download/>Exportar CSV</button></div></div>}
