import { ArrowDownRight,ArrowUpRight,LucideIcon } from 'lucide-react';
export function KpiCard({label,value,change,detail,icon:Icon,tone='teal'}:{label:string;value:string;change?:number;detail:string;icon:LucideIcon;tone?:string}){return <article className="kpi-card"><div className={`kpi-icon ${tone}`}><Icon/></div><div className="kpi-head"><span>{label}</span>{change!==undefined&&<b className={change>=0?'positive':'negative'}>{change>=0?<ArrowUpRight/>:<ArrowDownRight/>}{Math.abs(change)}%</b>}</div><strong className="kpi-value">{value}</strong><small>{detail}</small></article>}
