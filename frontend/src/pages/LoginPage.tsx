import { FormEvent,useState } from 'react'; import { ArrowRight,Eye,EyeOff,LockKeyhole } from 'lucide-react'; import { api } from '../services/api';import { BrandLogo } from '../components/BrandLogo';
export function LoginPage({onLogin}:{onLogin:()=>void}){const [username,setUsername]=useState('');const [password,setPassword]=useState('');const [show,setShow]=useState(false);const [error,setError]=useState('');const [busy,setBusy]=useState(false);async function submit(e:FormEvent){e.preventDefault();setBusy(true);setError('');try{await api.login(username,password);onLogin()}catch(err){setError(err instanceof Error?err.message:'No se pudo iniciar sesión')}finally{setBusy(false)}}return <main className="login-page"><section className="login-brand"><img className="login-hero-image" src="/assets/login-imprenta.png" alt="Equipamiento y datos de contacto de Rojas Impresiones"/><div className="login-brand-inner"><span className="eyebrow light">Sistema gerencial inteligente</span><h1>Datos reales para decisiones más claras.</h1><p>Ventas, clientes, proyecciones e informes en un único lugar.</p></div></section><section className="login-panel"><form className="login-card" onSubmit={submit}><div className="login-logo"><BrandLogo className="login-isotype"/><div><strong>ROJAS INTELLIGENCE</strong><small>Rojas Impresiones</small></div></div><div className="login-heading"><span>ACCESO SEGURO</span><h2>Bienvenido</h2><p>Ingresá tus credenciales para continuar.</p></div><label>Usuario<input autoFocus autoComplete="username" value={username} onChange={e=>setUsername(e.target.value)} placeholder="Tu usuario"/></label><label>Contraseña<div className="password-field"><input type={show?'text':'password'} autoComplete="current-password" value={password} onChange={e=>setPassword(e.target.value)} placeholder="Tu contraseña"/><button type="button" aria-label="Mostrar contraseña" onClick={()=>setShow(!show)}>{show?<EyeOff/>:<Eye/>}</button></div></label>{error&&<p className="form-error">{error}</p>}<button className="primary-button" disabled={busy}>{busy?'Ingresando…':'INICIAR SESIÓN'}<ArrowRight/></button><p className="secure-note"><LockKeyhole/> Acceso protegido. Tus credenciales se validan de forma segura.</p></form></section></main>}
