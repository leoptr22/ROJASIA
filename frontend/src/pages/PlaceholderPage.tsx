import { ArrowLeft,Construction } from 'lucide-react'; import { Link } from 'react-router-dom';
export function PlaceholderPage({section}:{section:string}){return <main className="page placeholder"><div className="placeholder-icon"><Construction/></div><span className="eyebrow">PRÓXIMA ENTREGA</span><h1>{section.charAt(0).toUpperCase()+section.slice(1)}</h1><p>La arquitectura y navegación de este módulo ya están preparadas. Su funcionalidad se incorporará de forma incremental después de validar esta primera etapa.</p><Link className="secondary-button" to="/"><ArrowLeft/> Volver al inicio</Link></main>}
